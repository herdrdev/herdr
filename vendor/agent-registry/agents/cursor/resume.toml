accepted_references = ["id"]
preferred_reference = "id"
strategy = "separate_flag"
token = "--resume"

[resume_options]
flags = ["-f", "--force", "--yolo", "--auto-review", "--trust"]
options = ["--model", "--mode", "--sandbox"]
