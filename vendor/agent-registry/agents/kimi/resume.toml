accepted_references = ["id"]
preferred_reference = "id"
strategy = "separate_flag"
token = "--session"

[resume_options]
flags = ["-y", "--yolo", "--auto", "--plan"]
options = ["-m", "--model", "--agent"]
